import React from 'react';
import PropTypes from 'prop-types';
import InfoTableCell from './infoTableCell';

const InfoTable = ({ color, width, height }) => {
  return <ul style={{}} />;
};

export default InfoTable;

InfoTable.propTypes = {};
